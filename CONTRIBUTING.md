# 参与 ellephants

## 提交知识内容

知识内容通过仓库的“提交原创内容”或“提交资源导览”Issue 表单投稿：

1. 选择领域、主分支和主题，填写标题、简介和正文；需要随页面提供下载的文件可拖入“附件”字段。
2. 系统检查表单结构，并标记为等待审核。
3. 维护者核对内容与授权后添加 `content-approved` 标签。
4. 系统自动确定目录与文件名，保存并校验附件，套用页面格式，完成构建检查并发布。

投稿者不需要编辑仓库文件，也不需要创建分支或 Pull Request。若检查失败，请根据 Issue 中的提示编辑表单内容；系统会自动重新检查。

原创投稿内容以 CC BY-SA 4.0 发布。引用或链接的第三方内容必须标明作者、来源和许可或使用依据。

## 修改站点架构

下列内容属于站点架构，使用人工分支和 Pull Request 修改：

- `docs/.vitepress/`
- `scripts/`
- `package.json` 与 `package-lock.json`
- `.github/`
- `config/taxonomy/`
- 学科目录、页面模板和内容规则

架构修改前从最新的 `main` 创建分支；提交 Pull Request 前运行：

```bash
npm ci
npm run build
```

同时检查桌面端、移动端、导航、内容校验和生产构建。
